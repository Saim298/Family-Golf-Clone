VanillaTilt.init(document.querySelector(".overlay"), {
    max: 25,
    speed: 400
});

//It also supports NodeList
VanillaTilt.init(document.querySelectorAll(".overlay"));

VanillaTilt.init(document.querySelector(".cards"), {
    max: 25,
    speed: 400
});

//It also supports NodeList
VanillaTilt.init(document.querySelectorAll(".cards"));